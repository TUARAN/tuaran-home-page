import OAuthAuthorizationComplete from './OAuthAuthorizationComplete'

export const metadata = { title: 'MCP 授权已完成', robots: { index: false, follow: false } }

export default function OAuthAuthorizationCompletePage() {
  return <OAuthAuthorizationComplete />
}
